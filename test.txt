#title := 'Simple math example'

print('what is 1/4 * 10?')


if (#answer ^= '')
	if (#answer = (1/4) * 10 )
		PRINT('Correct Answer!')
	ELSE
		PRINT("That is not the answer!")
	endif
endif

Request Fields(#answer)

raw_html <input name="button" type="submit" value="Submit">